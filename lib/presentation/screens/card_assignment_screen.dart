// lib/presentation/screens/card_assignment_screen.dart

import 'package:flutter/material.dart';

class CardAssignmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Implementa la lógica para asignar juegos a las cartas aquí
    return Scaffold(
      appBar: AppBar(title: Text("Asignar Juegos a Cartas")),
      body: Center(
        child: Text("Pantalla de Asignación de Juegos a Cartas"),
      ),
    );
  }
}
