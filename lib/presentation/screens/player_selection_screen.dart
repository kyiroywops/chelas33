// lib/presentation/screens/player_selection_screen.dart

import 'package:flutter/material.dart';

class PlayerSelectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Implementa tu lógica para seleccionar jugadores aquí
    return Scaffold(
      appBar: AppBar(title: Text("Seleccionar Jugadores")),
      body: Center(
        child: Text("Pantalla de Selección de Jugadores"),
      ),
    );
  }
}
